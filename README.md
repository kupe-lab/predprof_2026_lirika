# School Canteen API 🍽️

REST API для школьной столовой с системой авторизации, управления пользователями и питания.

## 📋 Содержание

- [Технологии](#технологии)
- [Структура проекта](#структура-проекта)
- [Быстрый старт (Docker)](#быстрый-старт-docker)
- [Запуск без Docker](#запуск-без-docker)
- [API Эндпоинты](#api-эндпоинты)
- [Переменные окружения](#переменные-окружения)
- [Развертывание](#развертывание)

---

## 🛠 Технологии

- **Backend:** Node.js, Express, TypeScript
- **База данных:** PostgreSQL
- **Контейнеризация:** Docker, Docker Compose
- **Аутентификация:** JWT (JSON Web Tokens)

---

## 📁 Структура проекта

```
school-canteen-api/
├── src/                    # Исходный код
│   ├── routes/            # API маршруты
│   ├── middleware/        # Middleware (auth, roles)
│   ├── utils/             # Утилиты (db connection)
│   ├── types/             # TypeScript типы
│   ├── app.ts             # Express приложение
│   ├── server.ts          # Точка входа
│   └── init-db.ts         # Инициализация БД
├── dist/                  # Собранный JS код
├── Dockerfile             # 🔥 КОНФИГУРАЦИЯ DOCKER ОБРАЗА
├── docker-compose.yml     # 🔥 КОНФИГУРАЦИЯ DOCKER COMPOSE
├── package.json
├── tsconfig.json
└── .env                   # Переменные окружения (создать вручную)
```

> **📌 Важно:** Конфигурационные файлы Docker:
> - `Dockerfile` — описание образа приложения
> - `docker-compose.yml` — оркестрация контейнеров (приложение + база данных)

---

## 🚀 Быстрый старт (Docker)

### Требования

- Docker Desktop (https://www.docker.com/products/docker-desktop)
- PowerShell или терминал

### Установка и запуск

1. **Скопировать проект на компьютер**

2. **Перейти в папку проекта**
   ```powershell
   cd C:\путь\к\school-canteen-api
   ```

3. **Запустить контейнеры**
   ```powershell
   docker compose up -d
   ```

4. **Проверить логи**
   ```powershell
   docker compose logs -f
   ```

5. **Открыть в браузере**
   ```
   http://localhost:3000
   ```

### Управление контейнерами

```powershell
# Остановить контейнеры
docker compose down

# Перезапустить
docker compose restart

# Посмотреть статус
docker compose ps

# Удалить всё включая базу данных
docker compose down -v
```

---

## 💻 Запуск без Docker

### Требования

- Node.js 20+
- PostgreSQL 15+
- npm или yarn

### Установка

1. **Клонировать проект**
   ```bash
   git clone https://github.com/ВАШ_USERNAME/school-canteen-api.git
   cd school-canteen-api
   ```

2. **Установить зависимости**
   ```bash
   npm install
   ```

3. **Настроить базу данных PostgreSQL**
   
   Создать базу данных:
   ```sql
   CREATE DATABASE school_canteen;
   CREATE USER canteen_user WITH PASSWORD 'your_password';
   GRANT ALL PRIVILEGES ON DATABASE school_canteen TO canteen_user;
   ```

4. **Создать файл `.env`**
   ```env
   DB_HOST=localhost
   DB_PORT=5432
   DB_NAME=school_canteen
   DB_USER=canteen_user
   DB_PASSWORD=your_password
   JWT_SECRET=your_jwt_secret_change_me
   PORT=3000
   NODE_ENV=development
   ```

5. **Собрать проект**
   ```bash
   npm run build
   ```

6. **Инициализировать базу данных**
   ```bash
   npm run init-db
   ```

7. **Запустить сервер**
   ```bash
   npm start
   ```

---

## 📡 API Эндпоинты

### Аутентификация

| Метод | Эндпоинт | Описание |
|-------|----------|----------|
| POST | `/api/auth/register` | Регистрация пользователя |
| POST | `/api/auth/login` | Вход в систему |
| POST | `/api/auth/logout` | Выход из системы |

### Профиль

| Метод | Эндпоинт | Описание | Токен |
|-------|----------|----------|-------|
| GET | `/api/profile` | Получить профиль текущего пользователя | ✅ |
| GET | `/api/profile/admin` | Получить профиль администратора | ✅ (ADMIN) |

### Пользователи (только для админа)

| Метод | Эндпоинт | Описание | Токен |
|-------|----------|----------|-------|
| GET | `/api/users` | Получить всех пользователей | ✅ (ADMIN) |
| GET | `/api/users/:id` | Получить пользователя по ID | ✅ (ADMIN) |
| PUT | `/api/users/:id` | Обновить пользователя | ✅ (ADMIN) |
| DELETE | `/api/users/:id` | Удалить пользователя | ✅ (ADMIN) |

### Питание (Nutrition Logs)

| Метод | Эндпоинт | Описание | Токен |
|-------|----------|----------|-------|
| GET | `/api/nutrition/logs` | Получить записи питания | ✅ |
| POST | `/api/nutrition/logs` | Создать запись питания | ✅ |
| GET | `/api/nutrition/logs/:id` | Получить запись по ID | ✅ |
| PUT | `/api/nutrition/logs/:id` | Обновить запись | ✅ (ADMIN/OWNER) |
| DELETE | `/api/nutrition/logs/:id` | Удалить запись | ✅ (ADMIN/OWNER) |

---

## ⚙️ Переменные окружения

| Переменная | Описание | По умолчанию |
|------------|----------|--------------|
| `DB_HOST` | Хост базы данных | `db` (Docker) / `localhost` |
| `DB_PORT` | Порт базы данных | `5432` |
| `DB_NAME` | Имя базы данных | `school_canteen` |
| `DB_USER` | Пользователь БД | `canteen_user` |
| `DB_PASSWORD` | Пароль БД | `canteen_password` |
| `JWT_SECRET` | Секрет для JWT токенов | **Обязательно изменить!** |
| `PORT` | Порт приложения | `3000` |
| `NODE_ENV` | Окружение | `production` (Docker) / `development` |

---

## 🌐 Развертывание

### VPS (Ubuntu)

1. **Подключиться к VPS**
   ```bash
   ssh root@ВАШ_IP_VPS
   ```

2. **Установить Docker**
   ```bash
   curl -fsSL https://get.docker.com | sh
   usermod -aG docker $USER
   ```

3. **Загрузить проект**
   ```bash
   git clone https://github.com/ВАШ_USERNAME/school-canteen-api.git
   cd school-canteen-api
   ```

4. **Запустить**
   ```bash
   docker compose up -d
   ```

5. **Открыть порт в firewall**
   ```bash
   ufw allow 3000/tcp
   ```

6. **Доступ**
   ```
   http://ВАШ_IP_VPS:3000
   ```

### Render.com (бесплатно)

1. Загрузить проект на GitHub
2. Создать аккаунт на Render.com
3. Подключить GitHub репозиторий
4. Настроить переменные окружения в Render
5. Развернуть

---

## 🔧 Команды разработки

```bash
# Установка зависимостей
npm install

# Сборка TypeScript
npm run build

# Режим разработки с hot reload
npm run dev

# Запуск продакшн версии
npm start

# Инициализация базы данных
npm run init-db

# Проверка пользователей
npm run check-users
```

---

## 📝 Лицензия

MIT

---

## 👨‍💻 Автор

Твоё имя

---

## 🐛 Проблемы?

Если возникли проблемы:

1. Проверьте логи: `docker compose logs -f`
2. Убедитесь, что Docker Desktop запущен
3. Проверьте, что порт 3000 не занят
4. Удалите контейнеры и пересоберите: `docker compose down && docker compose up -d --build`

---

**🎉 Приятного использования!**
